name: Update Quotes Daily (Fast)

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
permissions:
  contents: write

jobs:
  update_quotes:
    name: ⚡ Fast Updater
    runs-on: ubuntu-latest
    
    steps:
      - name: 🛠️ Checkout
        uses: actions/checkout@v4
        
      - name: 🏃 Run Script
        id: run_script
        run: python scripts/update.py
        
      - name: 👀 Check Changes
        if: success()
        run: |
          git status
          echo ""
          echo "📊 数据统计:"
          wc -l quotes.csv || true

      - name: 📝 Commit & Push
        if: success()
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add quotes.csv
          if git diff --staged --quiet; then
            echo "✨ 没有变化，跳过提交。"
          else
            git commit -m "📅 [Fast Auto] Update quotes.csv $(date +'%Y-%m-%d %H:%M')"
            git push
          fi
